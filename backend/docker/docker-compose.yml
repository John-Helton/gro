version: '3'

services:
  projecttravel:
    build:
      context: ..
      dockerfile: docker/dockerfile
    container_name: project-travel
    ports:
      - "8081:5000"
    env_file:
      - ../.env
    networks:
      - my-network
networks:
  my-network:
    driver: bridge